# Backend Service

This directory is reserved for the portfolio backend API service.

## Upcoming Milestones
- [ ] Initialize runtime and package manager (Issue #4)
- [ ] Implement healthcheck endpoint (`GET /api/health`)
- [ ] Add contact form handling / email dispatcher or dynamic data services
